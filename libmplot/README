The libmplot library is used to take simple commands and render them to a raster. This is useful for drawing graphs and other things...

The interface is based on the graphics operations available in PDF documents.
